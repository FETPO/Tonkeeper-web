{
  "name": "@tonkeeper/core",
  "version": "0.1.0",
  "author": "Nikita Kuznetsov <nkuz915@gmail.com>",
  "description": "Core features and state for tonkeeper web wallet",
  "main": "index.js",
  "scripts": {
    "build": "rm -fr dist && yarn generate:api && tsc",
    "start": "tsc -w",
    "generate:api": "npx swagger-typescript-api -p https://tonapi.io/swagger/swagger.json -o ./src/api --axios --api-class-name TonApi"
  },
  "license": "ISC",
  "devDependencies": {
    "typescript": "^4.9.3"
  },
  "dependencies": {
    "axios": "0.25.0",
    "ton": "^13.0.0",
    "ton-core": "^0.27.0",
    "ton-crypto": "^3.2.0"
  }
}
