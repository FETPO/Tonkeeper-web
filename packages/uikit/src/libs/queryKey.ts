export enum QueryKey {
  account = 'account',
  wallet = 'wallet',
  addresses = 'addresses',
  info = 'info',
  jettons = 'jettons',
  nft = 'nft',
}

export enum JettonKey {
  info,
  history,
  balance,
}
