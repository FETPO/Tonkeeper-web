export enum QueryKey {
  account = 'account',
  wallet = 'wallet',
  lock = 'lock',
  password = 'password',
  addresses = 'addresses',
  info = 'info',
  jettons = 'jettons',
  nft = 'nft',
  activity = 'activity',
  tonkeeperApi = 'tonkeeperApi',
}

export enum JettonKey {
  info,
  history,
  balance,
}

export enum TonkeeperApiKey {
  config,
  stock,
}
